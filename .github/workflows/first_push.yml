name: My First Push
on: 
  push:
  
jobs:
  Tarea1:
    runs-on: ubuntu-22.04

    steps:
      - name: Dastos del Push
        run: |
          echo "Autor de Push ${{github.event.head_commit.author}}"
          echo "Mensaje ${{github.event.head_commit.message}}"
